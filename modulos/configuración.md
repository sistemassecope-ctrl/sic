En el menu de configuración, añade otra opción que diga "Areas para PAO", aqui vas a crear un formulario que despliegue en forma de arbol el contenido de la tabla "area", el formulario permitirá arrastrar a un listado las áreas seleccionadas del listado anterior.  cuando se guarden se actualizarán estos datos arrastrados en una nueva tabla llamada "area_pao". crea la tabla "area_pao" con los siguientes campos:

id
area_id

agrega los campos de auditoria (created_at, updated_at, deleted_at).